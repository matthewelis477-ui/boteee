services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: botee
      POSTGRES_PASSWORD: botee
      POSTGRES_DB: botee
    ports:
      - "5432:5432"
    volumes:
      - botee_pg:/var/lib/postgresql/data

volumes:
  botee_pg:
